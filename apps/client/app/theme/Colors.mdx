import { Meta, ColorPalette, ColorItem } from "@storybook/blocks";
import colors from "./colors";

<Meta title="theme/Colors" />

# Colors

- [Background](#background)
- [Text](#text)
- [Border](#border)
- [Chromatic](#chromatic)

export function Template({ name }) {
  return (
    <ColorPalette>
      {Object.entries(colors[name]).map(([key, value]) => (
        <ColorItem key={key} title={`${name}.${key}`} colors={value} />
      ))}
    </ColorPalette>
  );
}

## Background

<Template name="background" />

## Text

<Template name="text" />

## Border

<Template name="border" />

## Chromatic

<Template name="chromatic" />
